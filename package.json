{
  "name": "preventah",
  "version": "1.0.0",
  "private": true,
  "description": "Preventah - a Nimiq Pay Mini App that turns hereditary health risk into a daily prevention habit, backed by a USDT commitment stake.",
  "license": "MIT",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "db:init": "node --env-file=.env.local --import ./scripts/register.mjs scripts/db-init.mjs",
    "test": "node --import ./scripts/register.mjs --test \"tests/**/*.test.mjs\""
  },
  "dependencies": {
    "@neondatabase/serverless": "1.1.0",
    "@nimiq/mini-app-sdk": "0.1.0",
    "next": "16.3.5",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "server-only": "0.0.1",
    "viem": "2.56.3"
  },
  "devDependencies": {
    "@types/node": "22.20.2",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
